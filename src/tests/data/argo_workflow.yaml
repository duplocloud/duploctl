workflow:
  metadata:
    generateName: duploctl-test-wf-
  spec:
    entrypoint: hello
    templates:
      - name: hello
        container:
          image: alpine:latest
          command: [echo]
          args: ["Hello from duploctl workflow test!"]
