# DuploCloud Configuration Example
# Copy this file to .env and update with your actual values
# IMPORTANT: .env is gitignored - use it for secrets and tokens!
#
# NOTE: Do NOT use 'export' keyword - direnv's dotenv_if_exists automatically
# exports all variables, and Docker's --env-file doesn't support 'export'

# ==============================================================================
# Python Configuration
# ==============================================================================

# Python version to use (default: 3.10)
PY_VERSION=3.10

# Optional: Enable Python venv creation in setup.sh (default: not set)
# Uncomment to create a virtual environment in .venv directory
# DUPLO_USE_VENV=1

# ==============================================================================
# DuploCloud Portal Configuration
# ==============================================================================

# Infrastructure name
DUPLO_INFRA="oteltest"

# Context name (portal identifier)
DUPLO_CONTEXT="test24"

# ==============================================================================
# DuploCloud Configuration Paths
# ==============================================================================

# Main configuration file
DUPLO_CONFIG="./config/duploconfig.yaml"

# DuploCloud home directory
DUPLO_HOME="./config"

# Cache directory for temporary files
DUPLO_CACHE="./config/cache"

# ==============================================================================
# AWS Configuration
# ==============================================================================

# AWS configuration file location
AWS_CONFIG_FILE="./config/aws"

# Kubernetes configuration
KUBECONFIG="./config/kubeconfig.yaml"

# AWS region
AWS_REGION="us-east-2"

# AWS profile to use
AWS_PROFILE="qa-aws"

# ==============================================================================
# DuploCloud CLI Settings
# ==============================================================================

# Output format (yaml, json, table)
DUPLO_OUTPUT="yaml"

# Log level (DEBUG, INFO, WARNING, ERROR)
DUPLO_LOG_LEVEL="DEBUG"

# ==============================================================================
# Docker Image Configuration
# ==============================================================================

# Docker image name
IMAGE="duplocloud/duploctl"

# Git information is set automatically in .envrc using command substitution
# (Docker --env-file doesn't support command substitution)

# ==============================================================================
# Python Path
# ==============================================================================

PYTHONPATH=".:${PYTHONPATH}"

# ==============================================================================
# SECRETS - Keep these in .env only!
# ==============================================================================

# DuploCloud Portal Credentials
# Option 1: Use 1Password CLI to load securely
# DUPLO_TOKEN=$(op read 'op://Private/Duplo QA Token/credential')
# Option 2: Set directly (NOT RECOMMENDED for production)
# DUPLO_HOST="https://your-portal.duplocloud.net"
# DUPLO_TOKEN='your-token-here'
# DUPLO_TENANT="your-tenant-name"

# GitHub Personal Access Token (if needed)
# GITHUB_TOKEN='your-github-token-here'

# GitHub Actions Runner Token (for self-hosted runners)
# GH_RUNNER_TOKEN='your-runner-token-here'


