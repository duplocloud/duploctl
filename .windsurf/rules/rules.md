---
trigger: always_on
---
@../../.github/copilot-instructions.md

- Even if rules mention github copilot, those are also applicable for windsurf
- Run only unit tests locally using `pytest src -m unit`
- Do not run integration tests locally, unless asked explicitely
- Always use .venv to install the modules and run tests
