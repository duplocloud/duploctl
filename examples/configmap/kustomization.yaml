# yaml-language-server: $schema=https://json.schemastore.org/kustomization.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: config-example
  annotations:
    kubernetes.io/description: |
      An example creating a ConfigMap and passing through the duplo transformer.
generators:
- config.yaml

  
